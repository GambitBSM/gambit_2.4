
Parameters:

  # Dummy model to allow ColliderBit to perform a simple scan
  # by replacing entries in a SLHA file
  CB_SLHA_simpmod_scan_model:
    m1:
      prior_type: none
    m2:
      prior_type: none
    cross_section_fb:
      prior_type: none
    cross_section_uncert_fb:
      prior_type: none
    br1: 1.0
    br2: 1.0
#    dec_neut:
#      prior_type: none
#    dec_char:
#      prior_type: none

Priors:
  # All priors simple, so specified in Parameters section


Scanner:

  use_scanner: raster

  scanners:

    raster:
      plugin: raster
      like: LogLike
      parameters:
        "CB_SLHA_simpmod_scan_model::m1": [100.0]
        "CB_SLHA_simpmod_scan_model::m2": [1000.0]
        "CB_SLHA_simpmod_scan_model::cross_section_fb": [4.5]
        "CB_SLHA_simpmod_scan_model::cross_section_uncert_fb": [0.225]
        
Printer:

  #printer: cout

  printer: hdf5
  options:
    output_file: "CB_SLHA_scan_model.hdf5"
    group: "/CB_SLHA_scan_model"
    delete_file_on_restart: true


ObsLikes:

    # Combined LHC likelihood
    - purpose:    LogLike
      capability: LHC_Combined_LogLike

    # Observables (not included in likelihood)
    - purpose:    Observable
      capability: LHC_signals

    - purpose:    likelihood_details
      capability: LHC_LogLike_per_SR

    - purpose:    likelihood_details
      capability: LHC_LogLike_SR_indices

    - purpose:    Observable
      capability: LHCEventLoopInfo

    - purpose:    Observable
      capability: SLHAFileElements


Rules:

  ## Just use unweighted cross-sections
  - capability: EventWeighterFunction
    function: setEventWeight_unity

  # Read a single SLHA file and replace some entries for each
  # iteration of the scan
  - capability: SLHAFileNameAndContent
    function: getAndReplaceSLHAContent
    options:
      SLHA_filename: ColliderBit/data/param_card.SM.C1N2.Whall.slha
      replace_SLHA_keys:
        m1:
          - MASS;1000022;1
        m2:
          - MASS;1000023;1
          - MASS;1000024;1

  # List the SLHA file entries that should be stored in the GAMBIT output.
  # Use SLHAea syntax for the SLHA_keys: <block/pdg> ; <line identifier> ; <line index of requested element>
  - capability: SLHAFileElements
    function: getSLHAFileElements
    type: map_str_dbl
    options:
      value_for_missing_elements: -9999
      SLHA_keys:
        - MASS;1000022;1
        - MASS;1000024;1
        - NMIX;1,1;2
        - NMIX;1,2;2
        - NMIX;1,3;2
        - NMIX;1,4;2
        - 1000024;DECAY;2               # <-- Total decay width for 1000024 (chargino1)
        - 1000024;(any),2,1000022,24;0  # <-- BR for two-body decay 1000024 -> 1000022 24

  - capability: TotalCrossSection
    function: getYAMLCrossSection_param


  # Generate evens (don't read LH event files)
  - capability: HardScatteringEvent
    function: generateEventPythia
    module: ColliderBit
    options:
      drop_HepMC_file: false
      # Warning: the HepMC files can quickly become very large... (20 events take ~1MB)

  # Choose colliders to simulate and their convergence settings, and pick analyses to run with each collider.
  - capability: RunMC
    function: operateLHCLoop
    options:
      silenceLoop: false
      LHC_13TeV:
        min_nEvents: 50000
        max_nEvents: 50000
        events_between_convergence_checks: 50000000 # Just preventing it from trying to converge
        target_fractional_uncert: 0.3
        halt_when_systematic_dominated: true
        all_analyses_must_converge: false
        all_SR_must_converge: false
        maxFailedEvents: 500
        analyses:
          - ATLAS_13TeV_1Lep2b_139invfb

  # Choose Monte Carlo event simulator and options.
  - capability:  HardScatteringSim
    type: Py8Collider_defaultversion
    function: getPythia_SLHA 
    options:
      LHC_13TeV:
        xsec_veto: 0.007 #0.028
        pythia_settings:
          - SLHA:verbose = 1
          - Next:numberShowProcess = 1
          - Print:quiet = off
          - Next:numberCount = 1000
          - Beams:eCM = 13000
          - PartonLevel:MPI = off
          - PartonLevel:ISR = on
          - PartonLevel:FSR = on
          - HadronLevel:all = on
          #- SUSY:all = on
          #- SUSY:qqbar2chi0chi0 = on
          - SUSY:qqbar2chi+-chi0 = on
          #- SUSY:qqbar2chi+chi- = on
          - TauDecays:mode = 0
          - TimeShower:pTmin = 2
          # These settings should force the W boson to only decay to leptons. For validating a certain analysis
          - 24:onMode=off
          - 24:onIfAny = 13 11 # 15
          - 25:onMode=off # Forcing the higgs to decay to b bar
          - 25:onIfAny = 5

  - capability: HardScatteringSim
    type: const BaseCollider*
    function: getPythiaAsBase
    module: ColliderBit

  # Choose LHC likelihood form and options.
  - capability: LHC_LogLikes
    backends:
    - {capability: lnlike_marg_poisson_lognormal_error}
    options:
      use_FullLikes: true
      use_covariances: false
      combine_SRs_without_covariances: false
      covariance_marg_convthres_abs: 0.05
      covariance_marg_convthres_rel: 0.05
      covariance_nsamples_start: 100000

  # Choose options for the LHC likelihood combination
  - capability: LHC_Combined_LogLike
    options:
      skip_analyses: [ ]

  # Choose ATLAS detector simulation and options.
  - capability: ATLASDetectorSim
    options:
      LHC_13TeV:
        partonOnly: false
        antiktR: 0.4

# Set the names of key log files
Logger:

  redirection:
    [Debug] : "debug.log"
    [Default] : "default.log"
    [ColliderBit] : "ColliderBit.log"
    [Dependency Resolver] : "dep_resolver.log"

KeyValues:

  debug: true

  dependency_resolution:
    prefer_model_specific_functions: true

  # Choose a lower cutoff for the likelihood
  likelihood:
    model_invalid_for_lnlike_below: -5e5
    model_invalid_for_lnlike_below_alt: -1e5

  # Set the default output path
  default_output_path: "runs/CB_SLHA_file_model/"

  # We set the errors from the physics modules to be non-fatal
  # to avoid crashing the entire scan due to a rare problem point.
  # This means we should pay close attention to the log files...
  exceptions:
    ColliderBit_error: fatal
    DarkBit_error: fatal
    DecayBit_error: fatal
    FlavBit_error: fatal
    PrecisionBit_error: fatal
    SpecBit_error: fatal
